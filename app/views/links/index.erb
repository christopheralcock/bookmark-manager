<h1> Links </h1>

<ul id="links">
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL:   <a href="<%=link.url%>" target="_blank"><%=link.url%></a>
      tag:   <%= link.tags.map(&:name) %>

    </li>
  <% end %>
</ul>

<a href="/links/new">Add new link</a>
